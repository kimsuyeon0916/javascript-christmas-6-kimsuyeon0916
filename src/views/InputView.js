const InputView = {
  async readDate() {
    
    return Number(date);
  },

  async readMenu() {
    
    return menu;
  },

  async exceptionHandler(message, Validate) {
    
  },
};

export default InputView;
